// Forecast view — flow projections per origin country.
// Consumes /forecast/{country}; full implementation lands on feat/forecast-view.

export default function Forecast() {
  return (
    <div className="view view-forecast">
      <div className="view-placeholder">
        <h2>Forecast</h2>
        <p>Flow forecaster output, with prediction intervals — coming next.</p>
      </div>
    </div>
  );
}
